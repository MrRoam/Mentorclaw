services:
  mentorclaw-kernel:
    image: node:24-bookworm
    working_dir: /app
    command: ["npm", "run", "validate:runtime"]
    environment:
      mentorclaw_RUNTIME_ROOT: /runtime
    volumes:
      - ./:/app:ro
      - /path/to/runtime-instance:/runtime
